name: Deploy Backend to Railway

# Railway auto-deploys via GitHub integration on every push to master.
# This workflow is kept as documentation only.
# To manually deploy: cd backend && railway up --detach

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway auto-deploys from the GitHub integration. No manual action needed."
